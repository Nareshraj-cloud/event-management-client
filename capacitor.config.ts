import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aids.campusconnects',
  appName: 'Campus Connects',
  webDir: 'build'
};

export default config;
